{
  "dependencies": {
    "express": "^4.21.1",
    "multer": "^1.4.5-lts.1",
    "gh-pages": "^6.2.0"
  },
  "devDependencies": {},
  "name": "upload-website",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "test": "mocha --reporter spec --check-leaks --bail test/",
    "build": "mkdir -p build && cp -r public/* build/ && cp app.js build/ && cp package.json build/ && cp package-lock.json build/",
    "deploy": "gh-pages -d build"
  },
  "homepage": "https://wilayat1114.github.io/project",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
