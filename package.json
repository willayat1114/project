{
  "dependencies": {
    "express": "^4.21.1",
    "gh-pages": "^6.2.0",
    "googleapis": "^133.0.0",
    "multer": "^1.4.5-lts.1"
  },
  "name": "upload-website",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "test": "mocha --reporter spec --check-leaks --bail test/",
    "build": "mkdir -p build && cp -r public/* build/ && cp app.js build/",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "homepage": "https://your-username.github.io/your-repo-name",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
