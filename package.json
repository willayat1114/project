{
  "dependencies": {
    "express": "^4.21.1",
    "multer": "^1.4.5-lts.1",
    "gh-pages": "^6.2.0"
  },
  "devDependencies": {},
  "name": "upload-website",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "test": "mocha --reporter spec --check-leaks --bail test/",
    "build": "mkdir build && xcopy /E /I public build && copy app.js build\\ && copy package.json build\\ && copy package-lock.json build\\",
    "deploy": "gh-pages -d build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
